# `manuscript/appendices/` — Agent Guide

Back-matter appendices (notation, math review, authoring guide, format gallery).

**Contents.** Each `appendix_*.md` carries a `{#sec:appendix_*}` label and is wired into `manuscript/config.yaml`.

**Contract.** Cross-reference appendices with `[@sec:appendix_*]`; keep the `appendices/README.md` index current.

See the project [`AGENTS.md`](../../AGENTS.md) and [`docs/`](../../docs/) for the full map.
