# `manuscript/assets/` — Agent Guide

Static manuscript assets (committed, not generated).

**Contents.** `cover/` holds the book cover image; `data/` holds small sample datasets the labs/figures load.

**Contract.** Only small, license-clean static inputs belong here; generated artifacts live under `output/`.

See the project [`AGENTS.md`](../../AGENTS.md) and [`docs/`](../../docs/) for the full map.
