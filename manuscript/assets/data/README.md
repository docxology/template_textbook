# `manuscript/assets/data/`

Small sample datasets used by labs and figures.

**Contents.** `sample_dataset.csv` is a tiny deterministic dataset loaded by the visualization/labs; `format_gallery.csv` backs the deliberately varied numeric table used to exercise Markdown rendering.

**Contract.** Keep datasets small and committed so the book renders without external downloads.

See the project [`AGENTS.md`](../../../AGENTS.md) and [`docs/`](../../../docs/) for the full map.
